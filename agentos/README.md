# Agent OS workspace

This directory is reserved for repository-local Agent OS definitions and managed
artifacts. Runtime formats will be introduced only after their domain contracts
and trust rules are specified in later stages.
